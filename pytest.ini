[pytest]
testpaths = tests
norecursedirs = audit .venv
markers =
    realdata: tests that require INVESTBOT_REALDATA_PATH environment variable
    integration: integration tests requiring env flags (SKIP in CI by default)
